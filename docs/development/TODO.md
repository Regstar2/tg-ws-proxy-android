# Future work

Items intentionally **not** done in doc-only releases (e.g. 1.8). Track ideas here instead of mixing into routing hotfixes.

## Runtime / routing

- Further unify legacy `ConnectionMode` with per-network `NetworkRoutePolicy` in UI (single mental model).
- Fake TLS / pinned TLS for GitHub CF domain fetch (research).
- One-click custom Cloudflare domain setup wizard.

## Tooling

- CI workflow for `assembleDebug` on push (optional).
- Stabilize `testDebugUnitTest` on all developer machines.

## Documentation

- Replace legacy `docs/screenshots/` with only `docs/images/` once all links are migrated.
- Add `screenshot-logs` and `screenshot-route-status` when captures are available in `readme_images/`.
